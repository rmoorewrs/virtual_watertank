# Docker compose for remote images

If your system has internet access and you have issues building the images locally, you can use this `docker-compose.yaml` file. It will pull the images from Docker Hub. 
